import { OpenAIApi } from 'openai';
import OpenAIResponse from "../model/OpenAIResponse";
export default class OpenAIRepository {
    openai: OpenAIApi;
    models: Map<string, number>;
    constructor();
    query(text: string): Promise<OpenAIResponse>;
    queryQuestions(questions: string[], errorCount?: number, model?: string, assistantMessages?: string[]): Promise<OpenAIResponse>;
    retryStatuses: number[];
}
