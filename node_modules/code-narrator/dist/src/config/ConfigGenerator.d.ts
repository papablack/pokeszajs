import ICodeNarratorConfig from "./ICodeNarratorConfig";
export default class ConfigGenerator {
    static generate(projectConfig?: Partial<ICodeNarratorConfig>): Promise<Partial<ICodeNarratorConfig>>;
    private static getDocPath;
}
