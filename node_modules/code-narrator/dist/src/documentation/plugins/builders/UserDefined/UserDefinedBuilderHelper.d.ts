import IBuilder from "../../../../config/IBuilder";
import IGenericeAIService from "../../../../services/OpenAIService";
export default class UserDefinedBuilderHelper {
    aiService: IGenericeAIService;
    constructor();
    extractPathFromContent(input: string): string | null;
    loadArgs(build: IBuilder, project_path: string): void;
    getAssistantMessages(builder: IBuilder): Promise<string[]>;
    private getPredefinedQuestion;
    extractContentBetweenHeaders(markdown: string): string;
    findFiles(patternString: string): import("../../../Document").default[];
}
