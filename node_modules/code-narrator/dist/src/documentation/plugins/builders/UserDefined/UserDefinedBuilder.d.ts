import BaseBuilder from "../BaseBuilder";
import IBuilder from "../../../../config/IBuilder";
export default class UserDefinedBuilder extends BaseBuilder {
    constructor();
    extractPathFromContent(input: string): string | null;
    generate(): Promise<void>;
    generateFromBuilder(builder: IBuilder, saveToPath: string, position: number, data: any): Promise<void>;
    private hasTemplateChanged;
    private getAffectedFilePaths;
}
