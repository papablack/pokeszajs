import FolderStructure from "../../../../utils/FolderStructure";
import BaseBuilder from "../BaseBuilder";
export default class FilesBuilder extends BaseBuilder {
    constructor();
    generate(): Promise<void>;
    queryForFolder(folder: FolderStructure): Promise<void>;
    private cleanDeletedFilesFromCache;
    private shouldDocument;
}
